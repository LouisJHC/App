<%= form_for([@post, @post.comments.build]) do |f| %>
	<%= f.label :name %><br>
	<%= f.text_field :name %><br>
	<br>
	<%= f.label :content %><br>
	<%= f.text_field :cont %><br>
	<br>
	<%= f.submit "write comment" %>
<% end %>